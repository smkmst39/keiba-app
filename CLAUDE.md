# 競馬予想ツール — プロジェクト概要

## このプロジェクトについて
JRA競馬レースの出走馬データをnetkeibaから取得し、
スコアと長期期待値を自動算出する競馬予想Webアプリ。

**最終目標**: raceIdを入力するだけで全券種の期待値付き
馬券フォーメーションシミュレーターが動作すること。

---

## 技術スタック

| 役割             | 技術                        |
|------------------|-----------------------------|
| フレームワーク   | Next.js 14 (App Router)     |
| 言語             | TypeScript（strictモード）  |
| パッケージ管理   | pnpm                        |
| スクレイピング   | axios + cheerio             |
| キャッシュ       | node-cache (TTL: 600秒)     |
| スタイリング     | Tailwind CSS                |
| テスト           | Vitest                      |
| CI               | GitHub Actions              |

---

## ディレクトリ構成

```
keiba-app/
├── app/
│   ├── api/race/[raceId]/route.ts   # メインAPIエンドポイント
│   ├── components/                  # UIコンポーネント
│   ├── hooks/                       # カスタムフック
│   └── page.tsx
├── lib/
│   ├── scraper/                     # データ取得層
│   │   ├── netkeiba.ts
│   │   └── types.ts
│   └── score/                       # スコア・期待値計算層
│       └── calculator.ts
├── scripts/                         # バックテスト・補正係数最適化
├── tests/                           # テストファイル
└── CLAUDE.md                        # ← このファイル
```

---

## コーディング規約

### 基本方針
- TypeScript strict モードを維持する。`any` 型は原則禁止
- 関数は単一責任。1関数100行を超えたら分割を検討する
- コメントは**日本語**で記述する（変数名・関数名は英語）
- `console.log` は開発用途のみ。本番コードには残さない

### 命名規則
```typescript
// 関数: camelCase
function calcScore(horse: Horse): number {}

// 型・インターフェース: PascalCase
type BetType = 'tan' | 'fuku' | ...

// 定数: UPPER_SNAKE_CASE
const MAX_CORRECTION = 0.20;

// Reactコンポーネント: PascalCase
export function BakenSimulator() {}
```

### エラーハンドリング
- try/catchで必ずエラーをキャッチし、ログを出力する
- スクレイピング失敗時はフィールドを0またはnullで埋め、
  処理を止めない（部分的な成功を許容する）
- APIルートは必ず `{ success: boolean, data, error? }` 形式で返す

---

## 環境変数

| 変数名                | 用途                              | デフォルト |
|-----------------------|-----------------------------------|------------|
| USE_MOCK              | trueのときモックデータを返す      | true       |
| CACHE_TTL_SECONDS     | キャッシュの有効期限（秒）        | 600        |
| SCRAPE_INTERVAL_MS    | スクレイピング間隔（ミリ秒）      | 1000       |
| NETKEIBA_SESSION_COOKIE | セッションCookie（将来用）      | -          |

---

## 絶対に守ること（禁止事項）

1. **`USE_MOCK=false` のままテストを実行しない**
   → 実ネットワークへの過剰アクセスになるため

2. **スクレイピング間隔を1秒未満にしない**
   → netkeiba のレート制限に引っかかるため

3. **`.env.local` をコミットしない**
   → `.gitignore` に必ず含める

4. **スコアの重みの合計を1.0から変えない**
   → 期待値の正規化が崩れるため

5. **期待値の補正係数（CORRECTION_FACTOR）を0.3超にしない**
   → 人気薄の期待値が異常値になるため（過去の失敗から学んだ制約）

---

## Git運用ルール

### 作業開始前の必須チェック
- 作業を始める前に必ず `git pull origin main` を実行すること
- ローカルとリモートが乖離するとマージ作業が発生する

### 週次スクレイプとの関係
- `.github/workflows/weekly-scrape.yml` が毎週火曜 08:00 JST に自動実行される
- 自動コミット例: `chore: weekly scrape YYYY-MM-DD (+NNR)`
- 火曜日以降の作業時は特に `git pull origin main` を忘れない

### push が rejected された場合の対処
1. `git pull origin main` でリモートの変更を取り込む
2. マージコミットメッセージはデフォルトのまま `:wq` で保存
3. `git push origin main` で再度 push

### 設定
- マージ方式: `git config pull.rebase false`（merge方式）
- 週次スクレイプとコード変更は別ファイルなので競合は起きにくい

---

## 実装前レビュー必須ルール

下記の「適用条件」に該当する変更は、本実装に着手する前に**下調べフェーズ**を挟み、
コミット無しで報告のみ行う。バランス重視: 軽微な変更は下調べを省略してよい。

### 適用条件（いずれかに該当したら下調べフェーズ必須）

1. 新規スクレイピング・パース処理を含む（実HTML/構造未確認の場合）
2. 既存のスコア計算・重み配分（WEIGHTS / CORRECTION_FACTOR 等）を変更する
3. バックテスト・検証スクリプトを新規作成する
4. 過去データの再取得・再収集を伴う
5. 推測に基づくセレクタ・データ構造を含む実装
6. 広範囲な挙動変更（複数モジュールに影響、または既存テストの期待値が大きく変わる見込み）

### スキップして良いケース（バランスモード）

下記は下調べフェーズ不要で直接実装してよい:

- typo / コメント修正
- 既存ロジック内の軽微なリファクタ（挙動不変）
- 単一ファイルの軽微な仕様追加（API シグネチャ無変更）
- 影響範囲が明確なバグ修正
- 既存テスト・ログ出力の調整

迷ったら下調べフェーズを挟む方を選ぶ。煩わしさより誤実装のほうがコストが高い。

### 下調べフェーズで確認すること

1. **前提の検証**: 推測ベースの構造・セレクタ・フィールドを **実物で確認** する
   （実HTMLを取得してパースを試す、verification JSON のサンプル抽出 等）
2. **タイムリーキ・データ整合性**: 過去のレースを「現在」のデータで再現できるか確認
   （バックテストでは未来情報の混入が起きうる）
3. **既存ロジックへの影響評価**: WEIGHTS 配分・閾値・健全性チェックの前提が崩れないか
4. **レート制限・コスト評価**: 何アクセス必要か、何分かかるか、事故歴はあるか

### 報告フォーマット

下調べフェーズの結果は以下を必ず含めて報告する:

- **想定通りだった点**: 推測が当たっていた範囲
- **想定と違った点**（最重要）: 推測が外れた箇所と影響
- **新たに発覚したリスク・懸念**
- **代替案を最低3つ提示**
- **推奨案と理由**

### 自己批判の許可

指示が **推測ベース** ・**実データ未確認** ・**既存ルールと矛盾** する場合は、
**必ず指摘すること**。「指示通りに動く」より「**正しく動くものを作る**」を優先する。
ストレート実装に走る前に、再相談を選ぶ。

### コミット粒度

- **下調べフェーズ**: コミットなし、報告のみ（scratch スクリプトは作業後に削除）
- **本実装**: 段階別に小コミット（型定義 → パーサー → ロジック → テスト の順）
- **既存挙動を変える変更**: 単独コミットに分離（後で revert しやすくする）

### 過去の事例（教訓）

- **2026-05-07: courseRecord (Phase 1-D)**:
  shutuba_past.html ベースで実装する仕様だったが、下調べで以下が判明:
  - shutuba_past は **着順を持たない**（凡例で「1着馬名(タイム差)」とのみ表記）
  - `/horse/{id}/` は競走成績テーブルなし、`/horse/result/{id}/` が別途必要
  - **推測実装していたら全馬 rank=undefined で片肺動作のまま発見遅延**
  → 案を `/horse/result/{id}/` 全頭叩きに切り替え、コミット 30bc455〜6264fea で完遂

- **2026-05-07: バックテスト時間整合性**:
  「過去レースの過去走を *現在* 取得すると未来情報が混入」（タイムリーキ）。
  100R バックテストは設計から見直しが必要となり別タスクへ分離。

- **2026-05-07: WEIGHTS 配分の前提崩壊**:
  Phase 2E Stage 3 の重み配分は「**courseRecord = 50 固定**」前提で導出されていた。
  courseRecord 実装で前提が崩れたため、Phase 2H として重み再チューニングが必要。

### 運用ルール

- 直近10件まで保持。古いものは `archive/claude_md_lessons.md` に移動する。
- 各事例は「日付・タスク名・推測実装の落とし穴・回避できた理由」を1段落でまとめる。

---

## フェーズ管理

| フェーズ | 内容                          | 状態     |
|----------|-------------------------------|----------|
| 1-A      | プロジェクト初期化・型定義    | 完了     |
| 1-B      | netkeibaスクレイパー実装      | 完了     |
| 1-C      | スコア・期待値計算ロジック    | 完了     |
| 1-D      | フロントエンドAPI連携         | 完了     |
| 2        | バックテスト・補正係数最適化  | 未着手   |
| 3        | 機械学習モデル導入            | 未着手   |

---

## 実装方針：シンプル優先

問題解決のアプローチは必ずシンプルな順に試すこと:

1. 既存のHTMLページから直接スクレイピング（最優先）
2. JSON APIが存在する場合はそちらを使う
3. 複数の方法がある場合は実装コードが少ない方を選ぶ

APIを探し回る前に、まず表示されているHTMLページから
取得できないか確認する。

---

## 表示データの方針：独自分析として表示する

### 背景
このツールはGoogle AdSense審査・広告収益化を目指している。
スクレイピングデータの無断転載はAdSense審査落ちの原因になる。

### 守ること
- netkeibaから取得した馬名・オッズ・騎手などの生データを
  そのまま一覧表示することは避ける
- 取得したデータはあくまで「計算の入力値」として使い、
  表示するのは「独自スコア・独自期待値・独自分析結果」にする
- ページの主役は「当ツール独自のスコアとEV」であることを明確にする
- JRA公式データへの参照リンクを適切に設置する

### 具体的な表現方針
- NG: 「netkeibaのオッズ: 4.7倍」
- OK: 「参考オッズ: 4.7倍（独自スコア: 88 / 期待値: 1.12）」

- NG: 出走馬一覧をnetkeibaと同じレイアウトで丸ごと表示
- OK: 独自スコア順・EV順で並び替えた分析結果として表示

### データ取得について
- スクレイピングは1秒以上の間隔を必ず守る
- 取得したデータをそのまま別サイトに転載しない
- あくまで「分析ツール」であることをサイト上で明示する

---

## 参照すべきCLAUDE.md

作業するディレクトリに応じて、以下のCLAUDE.mdも参照すること:
- `lib/scraper/CLAUDE.md` — スクレイパーを触るとき
- `lib/score/CLAUDE.md`   — スコア計算を触るとき
- `app/api/CLAUDE.md`     — APIルートを触るとき
- `app/components/CLAUDE.md` — UIを触るとき
- `scripts/CLAUDE.md`     — スクリプトを実行・編集するとき
